# Customer-Transaction-Risk-Pipeline
Build a reproducible data pipeline that ingests raw customer transaction data, cleans and validates it, generates analytical features, and trains an ML model to identify high-risk customers
